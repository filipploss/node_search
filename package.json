{
  "name": "search",
  "version": "1.0.0",
  "description": "search script",
  "main": "index.js",
  "scripts": {
    "start": "npm install && node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Filipp Loss",
  "license": "ISC",
  "dependencies": {
    "glob-ignore": "^1.0.2"
  }
}
